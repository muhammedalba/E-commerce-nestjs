<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{subject}}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 20px auto;
      background: #ffffff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    .header {
      background-color: #d9534f;
      color: white;
      text-align: center;
      padding: 10px;
      border-radius: 8px 8px 0 0;
    }
    .header.warning {
      background-color: #f0ad4e;
    }
    .content {
      padding: 20px;
      line-height: 1.6;
    }
    .footer {
      text-align: center;
      color: #777;
      font-size: 12px;
      margin-top: 20px;
    }
    .details-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }
    .details-table th, .details-table td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    .details-table th {
      background-color: #f9f9f9;
    }
  </style>
</head>
<body>
  <div class="container">
    {{#if (eq alertType "out_of_stock")}}
    <div class="header">
      <h2>🚨 Alert: Out of Stock</h2>
    </div>
    {{else}}
    <div class="header warning">
      <h2>⚠️ Alert: Low Stock</h2>
    </div>
    {{/if}}

    <div class="content">
      <p>Hello <strong>{{adminName}}</strong>,</p>
      
      {{#if (eq alertType "out_of_stock")}}
      <p>Please be advised that the following product is completely out of stock.</p>
      {{else}}
      <p>Please be advised that the stock for the following product is running low.</p>
      {{/if}}

      <table class="details-table">
        <tr>
          <th>Product Name</th>
          <td>{{productTitle}}</td>
        </tr>
        <tr>
          <th>SKU</th>
          <td>{{sku}}</td>
        </tr>
        <tr>
          <th>Remaining Stock</th>
          <td>{{stock}}</td>
        </tr>
      </table>

      <p>Please take appropriate action soon.</p>
    </div>

    <div class="footer">
      <p>&copy; {{year}} {{companyName}}. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
